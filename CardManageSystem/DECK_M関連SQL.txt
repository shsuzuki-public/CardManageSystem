--CLAN_Mテーブル作成
CREATE TABLE CLAN_M (
	CLAN_ID NUMBER(2,0) NOT NULL,
	CLAN_NAME VARCHAR2(20) NOT NULL,
	MEMBER_STATE_ID NUMBER(2,0) NOT NULL,
	CONSTRAINT PK_CLAN PRIMARY KEY(CLAN_ID)
);

--CLAN_M初期データ登録
INSERT INTO CLAN_M (
	CLAN_ID,
	CLAN_NAME,
	MEMBER_STATE_ID
)
VALUES(
	1,
	'シャドウパラディン',
	1
);
INSERT INTO CLAN_M (
	CLAN_ID,
	CLAN_NAME,
	MEMBER_STATE_ID
)
VALUES(
	2,
	'むらくも',
	2
);

--DECK_Mテーブル作成
CREATE TABLE DECK_M (
	DECK_ID NUMBER(3,0) NOT NULL,
	CLAN_ID NUMBER(2,0) NOT NULL,
	DECK_NAME VARCHAR2(20) NOT NULL,
	REGULATION_FLAG CHAR(1) NOT NULL,
	CONSTRAINT PK_DECK PRIMARY KEY(DECK_ID)
);

--DECK_M初期データ登録
INSERT INTO DECK_M (
	DECK_ID,
	CLAN_ID,
	DECK_NAME,
	REGULATION_FLAG
)
VALUES(
	1,
	1,
	'スタンダードシャドウ',
	'0',
);
INSERT INTO DECK_M (
	DECK_ID,
	CLAN_ID,
	DECK_NAME,
	REGULATION_FLAG
)
VALUES(
	2,
	2,
	'スタンダードむらくも',
	'0'
);
INSERT INTO DECK_M (
	DECK_ID,
	CLAN_ID,
	DECK_NAME,
	REGULATION_FLAG
)
VALUES(
	3,
	2,
	'Pスタンむらくも',
	'1',
);

--DECK_DETAIL_T作成
CREATE TABLE DECK_DETAIL_T (
	DECK_ID NUMBER(3,0) NOT NULL,
	CARD_ID NUMBER(9,0) NOT NULL,
	ADOPT_NUM VARCHAR2(20) NOT NULL,
	DECK_NAME VARCHAR2(20) NOT NULL,
	DECK_TYPE_FLAG CHAR(1),
	CONSTRAINT PK_DECK_DETAIL PRIMARY KEY(DECK_ID, CARD_ID)
);

--CARDマスタ作成
CREATE TABLE CARD_M (
	CARD_ID NUMBER(3,0) NOT NULL,
	CARD_NAME VARCHAR(20) NOT NULL,
	MEMBER_STATE_ID NUMBER(2,0) NOT NULL,
	CLAN_ID NUMBER(2,0),
	GRADE CHAR(1) NOT NULL,
	CARD_TYPE CHAR(2) NOT NULL,
	POSESSION VARCHAR(3),
	USE VARCHAR(3),
	CONSTRAINT PK_CARD PRIMARY KEY(CARD_ID)
);
